"use strict";
exports.__esModule = true;
var src_1 = require("../src");
describe('Given BREAK_ON_CHAR regex', function () {
    test('it splits by spaces', function () {
        var space = 'String with spaces';
        expect(src_1.BREAK_ON_CHAR.test(space)).toBeTruthy();
        expect(space.split(src_1.BREAK_ON_CHAR).length).toBe(3);
    });
    test('it splits by dashes', function () {
        var dash = 'String-with-dashes';
        expect(src_1.BREAK_ON_CHAR.test(dash)).toBeTruthy();
        expect(dash.split(src_1.BREAK_ON_CHAR).length).toBe(3);
    });
    test('it splits by whitespace', function () {
        var multiline = "String \n    with \n    spaces";
        expect(src_1.BREAK_ON_CHAR.test(multiline)).toBeTruthy();
        expect(multiline.split(src_1.BREAK_ON_CHAR).filter(function (word) { return Boolean(word); }).length).toBe(3);
    });
});
describe('Given REPLACE_CHAR regex', function () {
    var BAD = '%&*^$#*(&(^<keep-this.string/>';
    test('It replaces bad chars in the string', function () {
        expect(BAD.replace(src_1.REPLACE_CHAR, '')).toBe('%keep-this.string/');
    });
});
//# sourceMappingURL=regex.test.js.map