"use strict";
exports.__esModule = true;
exports.isNumber = exports.toNumber = exports.toFloat = void 0;
function toFloat(fraction, index) {
    var numerator = fraction.slice(0, index);
    var denominator = fraction.slice(index + 1);
    var total = Number(numerator) / Number(denominator);
    return total;
}
exports.toFloat = toFloat;
function toNumber(amount) {
    var regex = /\//gi;
    var regexData = regex.exec(amount);
    var isFraction = regexData === null || regexData === void 0 ? void 0 : regexData.index;
    if (isFraction) {
        return toFloat(amount, regexData === null || regexData === void 0 ? void 0 : regexData.index);
    }
    else {
        return Number(amount);
    }
}
exports.toNumber = toNumber;
function isNumber(current) {
    var regex = /\d/;
    return Boolean(current.match(regex));
}
exports.isNumber = isNumber;
//# sourceMappingURL=number.js.map